@page "/financeiro"
@inherits ListarFinanceiroPage

<MudText Typo="Typo.h3">Financeiros</MudText>

<div class="d-flex justify-end">
    <MudButton Variant="Variant.Filled"
               Color="Color.Primary"
               StartIcon="@Icons.Material.TwoTone.Add"
               Href="/financeiro/adicionar">
        NOVO FINANCEIRO
    </MudButton>
</div>

<MudDataGrid class="mt-4"
             Items="@Financeiros"
             Filterable="true"
             QuickFilter="Filter"
             Hideable="true">
    <ToolBarContent>
        <MudTextField @bind-Value="SearchTerm"
                      Placeholder="Filtrar..."
                      Adornment="Adornment.Start"
                      AdornmentIcon="@Icons.Material.Filled.Search"
                      Immediate="true"
                      IconSize="Size.Medium"
                      Class="mt-0" />
    </ToolBarContent>
    <Columns>
        <PropertyColumn Property="x => x.Id" Title="#" />
        <PropertyColumn Property="x => x.DataEmissao" Title="Dt. emissão" Format="dd/MM/yyyy" />
        <PropertyColumn Property="x => x.DataVencimento" Title="Dt. vencimento" Format="dd/MM/yyyy" />
        <PropertyColumn Property="x => x.Contrato.Paciente.Nome" Title="Paciente" />
        <PropertyColumn Property="x => x.Contrato.Responsavel.Nome" Title="Responsável" />
        <PropertyColumn Property="x => x.Valor" Title="Valor" Format="C" />
        <PropertyColumn Property="x => x.Situacao" Title="Situação" />
        <PropertyColumn Property="x => x.TipoCredito" Title="Tipo de crédito" />
        <PropertyColumn Property="x => x.TipoFinanceiro" Title="Tipo do financeiro" />
        <TemplateColumn Class="d-flex justify-end">
            <CellTemplate>
                <MudStack Row>
                    <MudIconButton Icon="@Icons.Material.Filled.Edit"
                                   Href="@($"/financeiro/editar/{context.Item.Id}")"
                                   Color="Color.Primary">
                    </MudIconButton>

                    <MudIconButton Icon="@Icons.Material.Filled.Delete"
                                   Color="Color.Error"
                                   arial-label="Excluir"
                                   OnClick="() => OnDeleteButtonClickedAsync(context.Item.Id,
                                                                             context.Item.Contrato.Paciente.Nome,
                                                                             context.Item.Contrato.Responsavel.Nome)">
                    </MudIconButton>
                </MudStack>
            </CellTemplate>
        </TemplateColumn>
    </Columns>
</MudDataGrid>